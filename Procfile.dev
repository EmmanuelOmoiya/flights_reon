# Procfile for development using HMR
# You can run these commands in separate shells
rails: bundle exec rails s -p 3000
flight-recons-client: bin/shakapacker-dev-server
flight-recons-server: SERVER_BUNDLE_ONLY=yes bin/shakapacker --watch